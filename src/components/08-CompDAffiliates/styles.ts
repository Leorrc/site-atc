import styled from "styled-components";

export const ContainerAffi = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
  margin-top: 2rem;

  :hover {
    cursor: pointer;
  }
`;